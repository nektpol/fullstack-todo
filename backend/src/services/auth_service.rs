#[allow(dead_code)]
pub struct AuthService;

#[allow(dead_code)]
impl AuthService {
    pub async fn register_user() {
        // logic later
    }

    pub async fn login_user() {
        // logic later
    }
}